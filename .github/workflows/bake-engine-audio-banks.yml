name: Bake engine audio banks

on:
  workflow_dispatch:
    inputs:
      preset_path:
        description: Optional res:// path to one EngineAudioBakePreset; leave empty to bake every registered preset
        required: false
        type: string
        default: ""

permissions:
  contents: write

env:
  GODOT_VERSION: 4.7-stable
  GODOT_ARCHIVE: Godot_v4.7-stable_win64.exe.zip
  GODOT_EXECUTABLE: Godot_v4.7-stable_win64_console.exe
  GODOT_CHECKSUMS_FILE: scripts/ci/godot_4_7_sha512.txt

jobs:
  bake:
    runs-on: windows-2025
    timeout-minutes: 20

    steps:
      - name: Check out selected branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Download and verify Godot
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $ProgressPreference = "SilentlyContinue"
          $toolsDirectory = Join-Path $PWD "tools/godot"
          New-Item -ItemType Directory -Path $toolsDirectory -Force | Out-Null
          $baseUrl = "https://github.com/godotengine/godot-builds/releases/download/$env:GODOT_VERSION"
          $archivePath = Join-Path $env:RUNNER_TEMP $env:GODOT_ARCHIVE
          Invoke-WebRequest -Uri "$baseUrl/$env:GODOT_ARCHIVE" -OutFile $archivePath
          $escapedArchiveName = [regex]::Escape($env:GODOT_ARCHIVE)
          $checksumLine = Get-Content -LiteralPath $env:GODOT_CHECKSUMS_FILE | Where-Object {
            $_ -match "^(?<hash>[0-9A-Fa-f]{128})\s+\*?$escapedArchiveName$"
          } | Select-Object -First 1
          if ([string]::IsNullOrWhiteSpace($checksumLine)) {
            throw "Pinned checksum manifest does not contain $env:GODOT_ARCHIVE."
          }
          $expectedHash = [regex]::Match($checksumLine, "^(?<hash>[0-9A-Fa-f]{128})").Groups["hash"].Value.ToLowerInvariant()
          $actualHash = (Get-FileHash -LiteralPath $archivePath -Algorithm SHA512).Hash.ToLowerInvariant()
          if ($actualHash -ne $expectedHash) {
            throw "Godot archive SHA512 mismatch."
          }
          Expand-Archive -LiteralPath $archivePath -DestinationPath $toolsDirectory -Force
          $godotPath = Join-Path $toolsDirectory $env:GODOT_EXECUTABLE
          "GODOT_BIN=$godotPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Import project resources
        shell: pwsh
        run: |
          & $env:GODOT_BIN --headless --path $PWD --import
          if ($LASTEXITCODE -ne 0) {
            throw "Godot resource import failed with exit code $LASTEXITCODE."
          }

      - name: Bake WAV banks
        shell: pwsh
        env:
          PRESET_PATH: ${{ inputs.preset_path }}
        run: |
          $ErrorActionPreference = "Continue"
          $diagnosticDirectory = Join-Path $PWD "build/engine-audio-bake"
          New-Item -ItemType Directory -Path $diagnosticDirectory -Force | Out-Null
          $logPath = Join-Path $diagnosticDirectory "bake.log"
          $arguments = @(
            "--headless",
            "--path", $PWD,
            "--script", "scripts/tools/bake_engine_audio_banks.gd"
          )
          if (-not [string]::IsNullOrWhiteSpace($env:PRESET_PATH)) {
            $arguments += @("--", "--preset=$env:PRESET_PATH")
          }
          & $env:GODOT_BIN @arguments 2>&1 | Tee-Object -FilePath $logPath
          $bakeExitCode = $LASTEXITCODE
          if ($bakeExitCode -ne 0) {
            throw "Engine-audio bank bake failed with exit code $bakeExitCode."
          }
          $wavFiles = @(Get-ChildItem -LiteralPath assets/audio/engine -Filter "*.wav" -File -Recurse)
          if ($wavFiles.Count -eq 0) {
            throw "Engine-audio bake completed without producing any WAV files."
          }
          $bankFiles = @(Get-ChildItem -LiteralPath assets/audio/engine -Filter "bank.tres" -File -Recurse)
          if ($bankFiles.Count -eq 0) {
            throw "Engine-audio bake completed without producing any bank resources."
          }

      - name: Import generated banks
        shell: pwsh
        run: |
          & $env:GODOT_BIN --headless --path $PWD --import
          if ($LASTEXITCODE -ne 0) {
            throw "Generated engine-audio import failed with exit code $LASTEXITCODE."
          }
          $missingImports = @(
            Get-ChildItem -LiteralPath assets/audio/engine -Filter "*.wav" -File -Recurse |
              Where-Object { -not (Test-Path -LiteralPath ($_.FullName + ".import") -PathType Leaf) }
          )
          if ($missingImports.Count -gt 0) {
            throw "Godot did not create import metadata for $($missingImports.Count) generated WAV file(s)."
          }

      - name: Upload bake diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: engine-audio-bake-${{ github.sha }}
          path: build/engine-audio-bake/
          if-no-files-found: warn
          retention-days: 14
          compression-level: 6

      - name: Commit generated banks
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- assets/audio/engine
          if (git diff --cached --quiet) {
            Write-Host "Baked banks already match the committed assets."
            exit 0
          }
          git commit -m "Bake engine audio banks"
          git push origin "HEAD:refs/heads/$env:GITHUB_REF_NAME"
