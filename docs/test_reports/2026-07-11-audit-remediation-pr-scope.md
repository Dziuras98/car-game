# Audit remediation pull-request scope

This change set implements all high-, medium- and low-priority corrections identified by the repository audit. The required Windows workflow is the release gate and the pull request must remain unmerged until its current head succeeds.
